# Core views — UI moved to UI module

UI components (layouts, ui/*, widgets) have been moved to `resources/views/ui` and are available as `x-ui::` (see `app/UI` module).

This directory is kept for backward compatibility only. Do not add new UI components here; use `resources/views/ui`.
